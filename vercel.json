{
    "buildCommand": "cd Price-Research && npm install && npm run build",
    "outputDirectory": "Price-Research/dist",
    "installCommand": "cd Price-Research && npm install",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}
